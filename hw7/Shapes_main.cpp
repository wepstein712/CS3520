#include "Vector2D.hpp"
#include "Shapes.hpp"
#include <memory>
#include <iomanip>
#include <iostream>
using namespace std;

int
main() {
  /*
  {
    // instantiating IShape doesn't compile
    //unique_ptr<IShape> ps1(new IShape());
  }

  {
    // create and print circle
    unique_ptr<IShape> ps1(new Circle(Vector2D(1.1, 2.2), 3.3));
    ps1->display(cout << setprecision(2)) << endl;
  }

  {
    // translate circle
    unique_ptr<IShape> ps1(new Circle(Vector2D(1.1, 2.2), 3.3));
    ps1->translate(Vector2D(1.1, 2.2));
    ps1->display(cout << setprecision(2)) << endl;
  }

  {
    // copying Circle doesn't compile
    //Circle c1 = Circle(Vector2D{}, 0);
  }
  
  {
    // copying Circle doesn't compile
    //Circle c1(Vector2D{}, 0);
    //c1 = Circle(Vector2D{}, 0);
  }

  {
    // create and print triangle
    unique_ptr<IShape> ps1(new Triangle(Vector2D(0.1, 0.1), Vector2D(0.1, 1.1), Vector2D(1.1, 0.1)));
    ps1->display(cout << setprecision(2)) << endl;
  }

  {
    // translate triangle
    unique_ptr<IShape> ps1(new Triangle(Vector2D(0.1, 0.1), Vector2D(0.1, 1.1), Vector2D(1.1, 0.1)));
    ps1->translate(Vector2D(1.1, 2.2));
    ps1->display(cout << setprecision(2)) << endl;
  }

  {
    // copying Triangle doesn't compile
    //Triangle c1 = Triangle(Vector2D{}, Vector2D{}, Vector2D{});
  }
  
  {
    // copying Triangle doesn't compile
    //Triangle c1(Vector2D{}, Vector2D{}, Vector2D{});
    //c1 = Triangle(Vector2D{}, Vector2D{}, Vector2D{});
  }
  */
}
