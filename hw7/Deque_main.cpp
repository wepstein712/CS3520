#include "Deque.hpp"
#include <algorithm>
#include <iostream>
#include <string>

int main() {
  /*
  {
    Deque<std::string> d;
    d.push_front("hello");
    d.debug_print();
  }

  {
    Deque<int> d;
    d.push_front(10);
    d.push_front(20);
    d.push_front(30);
    d.debug_print();
  }

  {
    Deque<int> d;
    d.push_front(10);
    d.push_front(20);
    d.pop_front();
    d.debug_print();
  }

  {
    Deque<int> d;
    d.push_front(10);
    d.push_front(20);
    d.pop_front();
    d.pop_front();
    d.pop_front();
    d.debug_print();
  }

  {
    Deque<std::string> d;
    d.push_back("hello");
    d.debug_print();
  }

  {
    Deque<int> d;
    d.push_back(10);
    d.push_back(20);
    d.push_back(30);
    d.debug_print();
  }

  {
    Deque<int> d;
    d.push_back(10);
    d.push_back(20);
    d.pop_back();
    d.debug_print();
  }

  {
    Deque<int> d;
    d.push_back(10);
    d.push_back(20);
    d.pop_back();
    d.pop_back();
    d.pop_back();
    d.debug_print();
  }

  {
    Deque<std::string> d;
    d.push_front("hello");
    d.push_back("world");

    std::cout << "range-based for:";
    for (auto elem: d) {
      std::cout << " " << elem;
    }
    std::cout << std::endl;
  }

  {
    Deque<std::string> d;
    d.push_back("1");
    d.push_back("2");

    std::cout << "find: " << ((std::find(d.cbegin(), d.cend(), "1") == d.cend()) ? "not found" : "found") << std::endl;
    std::cout << "find: " << ((std::find(d.cbegin(), d.cend(), "0") == d.cend()) ? "not found" : "found") << std::endl;
  }

  {
    Deque<int> d;
    d.push_back(1);
    d.push_back(2);
    d.push_back(3);
    d.push_back(4);

    std::cout << "for_each:";
    std::for_each(d.cbegin(), d.cend(), [](int x){ std::cout << " " << x; });
    std::cout << std::endl;
  }
  */
}
